{
  "name": "clahejs",
  "version": "1.0.0",
  "description": "JS implementation of CLAHE",
  "main": "clahe.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel --presets es2015 -d lib/ src",
    "start": "npm run build && node lib/clahe.js"
  },
  "author": "srie",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "numjs": "0.16.0"
  }
}
